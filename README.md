### Как поднять
1. Положить `bets.csv` и `events.csv` в `./data/`.
2. `docker-compose up -d`
3. Коннект: `localhost:5433`, user/pass: `betera/betera`.

### В директории sql_query находятся выполненные задания:Task 1.3.1, Task 1.3.2

### Pyspark файл: pyspark_task_1_3_1.ipynb